#Requires -Version 5.0

${config-ApplicationName} = $null
${config-ProductName} = $null
# Product Code is required for some resource names when ProductName is too long and should be a 2 or 3 character code
#   e.g.; cr - CapitalRaising
${config-ProductCode} = ${config-ProductName}
${config-ShortLineOfBusiness} = $null
